# The console talks only to our own API. Shopify credentials live in the
# backend and must never appear here — anything with VITE_ in the name is
# compiled into the bundle and readable by anyone who opens the page.
VITE_API_TARGET=http://localhost:8000
